@import 'nib'
@import '../../node_modules/jeet/jeet.styl'
@import '_mixins.styl'

// Media queries
$xl = 1600px
$lg = 1260px
$md = 1023px
$sm = 767px

// Colors
$primary ?= #fff
$secondary ?= #000

// Metrics
$padding = 2rem
$heading = 6rem
$kinetic = cubic-bezier(0.77, 0, 0.175, 1)
$containerTransition = 500ms

// CSS Reset
global-reset()

@import '_main.styl'
@import 'components/_outdated.styl'
@import 'components/_loader.styl'
@import 'components/_header.styl'
@import 'components/_sections.styl'
@import 'components/_page-panel.styl'

#main
	span(1)

#container
	span(1)
	transition transform $containerTransition $kinetic

@import 'components/_project.styl'