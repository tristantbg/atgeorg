@import 'nib'
@import '../../node_modules/jeet/jeet.styl'
@import '_mixins.styl'

// Media queries
$xl = 1600px
$lg = 1260px
$md = 1023px
$sm = 767px

// Colors
$primary ?= #fff
$secondary ?= #000

// Metrics
$padding = 2rem
$heading = 6rem
$kinetic = cubic-bezier(0.77, 0, 0.175, 1)

// CSS Reset
global-reset()

@import '_main.styl'
@import 'components/_outdated.styl'
@import 'components/_loader.styl'
@import 'components/_header.styl'
@import 'components/_sections.styl'

#main
	span(1)

#container
	span(1)

@import 'components/_project-slider.styl'